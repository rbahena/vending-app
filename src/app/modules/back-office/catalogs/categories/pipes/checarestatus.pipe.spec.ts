import { ChecarestatusPipe } from './checarestatus.pipe';

describe('ChecarestatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ChecarestatusPipe();
    expect(pipe).toBeTruthy();
  });
});
